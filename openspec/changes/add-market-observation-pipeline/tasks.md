## 1. Скелет решения и гейты

- [ ] 1.1 Создать `EveTrader.slnx`, `Directory.Build.props` / `.targets` / `.Packages.props` (TFM, nullable, `TreatWarningsAsErrors`, централизованные версии) — проверка: `dotnet build EveTrader.slnx -c Debug` проходит с нулём warning'ов
- [ ] 1.2 Создать проекты слоёв по `.agents/rules/csharp/local-project-layers.md`: `src/domain/EveTrader.Domain`, `src/application/EveTrader.Application`, `src/infrastructure/EveTrader.Infrastructure.{Esi,Facts,Sde}`, `src/presentation/EveTrader.Cli` — проверка: `dotnet sln EveTrader.slnx list` показывает все проекты, диф списка csproj и содержимого slnx пуст
- [ ] 1.3 Создать тестовые проекты в `tests/unit/`, `tests/integration/`, `tests/architecture/` на фиксированном стеке (xUnit v3, Shouldly, NSubstitute, Coverlet) — проверка: `dotnet test EveTrader.slnx` запускается и завершается успешно
- [ ] 1.4 Закрепить законы слоёв в `tests/architecture/` через NetArchTest (ссылки только вниз, домен без IO, инфраструктурные проекты не ссылаются друг на друга) — проверка: временно добавленная ссылка `EveTrader.Domain` → `EveTrader.Infrastructure.Esi` роняет тест, после удаления тесты зелёные
- [ ] 1.5 Подключить `Microsoft.CodeAnalysis.BannedApiAnalyzers` в `EveTrader.Domain` с запретом `DateTime.UtcNow`, `DateTimeOffset.Now`, `DateTime.Now`, `TimeProvider.System` — проверка: временный вызов `DateTime.UtcNow` в домене роняет сборку, после удаления сборка зелёная
- [ ] 1.6 Завести `CHANGELOG.md` в корне как аналог changelog из `process/docs-completeness.md` — проверка: файл содержит запись об этом изменении со ссылкой на change

## 2. Домен: типы и свёртка

- [ ] 2.1 Описать представление ордера и наблюдения: `struct` фиксированного размера, цена целым в сотых долях ISK, время наблюдения аргументом — проверка: unit-тесты на границы масштаба цены (максимальная цена ордера не переполняет), тест на размер структуры
- [ ] 2.2 Реализовать дифф двух наблюдений региона в события жизни ордера — проверка: unit-тесты покрывают все сценарии `specs/market-observation/order-events` (появление, цена, остаток, исчезновение, отсутствие события при отсутствии изменений)
- [ ] 2.3 Реализовать базовую линию: первое наблюдение региона даёт события отдельного вида — проверка: unit-тест, что после включения региона ни одно событие не помечено появлением
- [ ] 2.4 Реализовать правило частичного наблюдения: события исчезновения не порождаются — проверка: unit-тест на наблюдении с недополученными страницами
- [ ] 2.5 Реализовать вычисление признаков стакана (лучшие цены, спред, число ордеров, глубина по набору порогов) в той же проходке, что дифф — проверка: unit-тесты на двусторонний и односторонний стакан; отсутствие стороны отличимо от нулевой цены
- [ ] 2.6 Реализовать восстановление состава стакана из базовой линии и событий — проверка: property-тест «восстановить стакан на время наблюдения и пересчитать признаки — совпадает с признаками, посчитанными в проходке»
- [ ] 2.7 Замерить проходку на синтетическом стакане в миллион ордеров — проверка: тест на отсутствие аллокаций на ордер (`GC.GetAllocatedBytesForCurrentThread` не растёт линейно по числу ордеров), длительность зафиксирована в `CHANGELOG.md` или отчёте задачи

## 3. Инфраструктура ESI

- [ ] 3.1 Реализовать клиент стакана: пагинация по объявленному числу страниц, время получения на каждую страницу, границы интервала сбора на наблюдение — проверка: integration-тест против записанных ответов, время страниц различается
- [ ] 3.2 Реализовать governor бюджета ошибок, общий на процесс: чтение остатка и срока сброса, пауза при исчерпании — проверка: unit-тесты на паузу до сброса и возобновление, тест что бюджет один на все регионы
- [ ] 3.3 Реализовать условные запросы: хранение валидаторов кэша, обработка ответа «не изменилось» — проверка: integration-тест, что ответ 304 даёт запись покрытия и ноль событий
- [ ] 3.4 Реализовать потоковый разбор ответа через `Utf8JsonReader` прямо в переиспользуемый массив структур — проверка: тест на отсутствие аллокаций на ордер, тест на корректность разбора всех полей ордера
- [ ] 3.5 Реализовать обработку незавершённого наблюдения: пометка частичным с долей полученных страниц — проверка: integration-тест на обрыве источника посреди пагинации
- [ ] 3.6 Прогнать живой сбор одного региона (The Forge) — проверка: получены страницы, зафиксированы замеры числа запросов, объёма трафика, длительности наблюдения

## 4. Озеро фактов

- [ ] 4.1 Описать схемы Parquet для событий, базовых линий, признаков, дневной истории и покрытия; в каждой — два времени (событие/наблюдение и время получения) и версия статических данных где применимо — проверка: тест на чтение записанного файла и совпадение схемы с ожидаемой
- [ ] 4.2 Реализовать запись наблюдения: временный файл, атомарное переименование, запись покрытия последней — проверка: integration-тест, что прерывание до записи покрытия оставляет озеро без частичного наблюдения
- [ ] 4.3 Реализовать идемпотентность приёма — проверка: integration-тест, что повторная подача того же наблюдения не меняет состав фактов
- [ ] 4.4 Реализовать чтение через DuckDB: запрос на момент времени и запрос без момента — проверка: integration-тесты сценариев `specs/market-facts/bitemporal-store` (бэктест не видит будущего; без момента возвращается последняя версия)
- [ ] 4.5 Реализовать отказ на изменение и удаление факта и отказ на настройку срока хранения сырья — проверка: integration-тесты обеих операций
- [ ] 4.6 Реализовать партиционирование по дате наблюдения и региону и сортировку внутри файла — проверка: план запроса DuckDB за интервал читает только партиции интервала
- [ ] 4.7 Реализовать перестройку признаков из сырья — проверка: integration-тест «удалить признаки за интервал, перестроить, результат совпадает с удалённым»

## 5. Покрытие, охват, управление

- [ ] 5.1 Реализовать журнал покрытия: запись на каждую попытку наблюдения с исходом и границами интервала — проверка: integration-тесты на успех, «не изменилось», частичный сбор и отказ
- [ ] 5.2 Реализовать различение непокрытия и отсутствия изменений в запросах состояния — проверка: integration-тесты сценариев `specs/market-observation/coverage-log`
- [ ] 5.3 Реализовать политику охвата и темпа на регион с записью изменений политики со временем вступления в силу — проверка: integration-тест, что включение региона даёт запись политики и базовую линию
- [ ] 5.4 Реализовать планировщик: срок следующего наблюдения региона как максимум из срока годности источника и интервала политики; сообщение о несогласованной настройке — проверка: unit-тесты сценариев `specs/market-observation/scope-policy`
- [ ] 5.5 Реализовать отчёт о покрытии за интервал (доля покрытого времени, пропуски, частичные наблюдения) — проверка: integration-тест на интервале с искусственным пропуском
- [ ] 5.6 Реализовать команды `EveTrader.Cli`: запуск сбора, отчёт о покрытии, осмотр фактов, импорт, реплей — проверка: каждая команда запускается на тестовом озере и печатает непустой результат

## 6. Полный охват

- [ ] 6.1 Включить полный охват k-space и прогнать сутки — проверка: покрытие по всем регионам не ниже объявленного порога, процесс не падает
- [ ] 6.2 Снять фактические замеры: пиковая память, длительность наблюдения крупнейшего региона, объём сырья на диске за сутки, доля изменившихся ордеров за наблюдение, доля ответов «не изменилось», входящий трафик — проверка: замеры записаны и сопоставлены с бюджетом из `design.md`; расхождение больше чем втрое — повод пересмотреть бюджет, а не удалять сырьё

## 7. Дневная история и bulk-импорт

- [ ] 7.1 Реализовать сбор дневной истории с временем получения и партиционированием по нему — проверка: integration-тест, что повторное получение той же календарной даты даёт новую строку, а прежняя остаётся доступной
- [ ] 7.2 Реализовать импорт внешних исторических дампов в ту же схему через тот же вход — проверка: integration-тест, что импортированные и собранные факты неразличимы по схеме и лежат в одном наборе
- [ ] 7.3 Проверить открытый вопрос о рынках игровых структур: присутствует ли локация Перимитера в региональном стакане The Forge — проверка: результат замера записан в `design.md` §Open Questions и вопрос закрыт
- [ ] 7.4 Проверить открытый вопрос о фактическом числе торгуемых типов — проверка: число с ненулевым дневным объёмом посчитано по собранной истории и записано в `design.md`

## 8. Реплей

- [ ] 8.1 Реализовать порт источника наблюдений и реплей собственного архива через него — проверка: integration-тест, что реплей проходит те же стадии, что живой сбор, без ветвлений по типу источника
- [ ] 8.2 Сверить реплей с живым прогоном — проверка: реплей архива суток даёт побитово тот же набор событий и признаков, что был записан живым прогоном
- [ ] 8.3 Проверить детерминированность — проверка: два реплея одного архива дают идентичный результат, включая порядок строк

## 9. Приёмка

- [ ] 9.1 Реализовать метрики расхода и наблюдения по контракту `observability/diagnostics.md` — проверка: метрики числа запросов, трафика, доли «не изменилось», длительности наблюдения и остатка бюджета ошибок видны в экспорте
- [ ] 9.2 Прогнать приёмочный сценарий: сутки полного охвата, затем перестроить признаки из сырья и сверить с записанными — проверка: совпадение полное, отчёт о покрытии без незапланированных пропусков
- [ ] 9.3 Закрыть бумагу изменения — проверка: `openspec validate add-market-observation-pipeline --strict` без замечаний, запись в `CHANGELOG.md` на месте, `dotnet build EveTrader.slnx -c Debug` зелёный
