## 1. Скелет решения и гейты

- [ ] 1.1 Создать `EveTrader.slnx`, `Directory.Build.props` / `.targets` / `.Packages.props` (TFM, nullable, `TreatWarningsAsErrors`, централизованные версии) — проверка: `dotnet build EveTrader.slnx -c Debug` проходит с нулём warning'ов
- [ ] 1.2 Создать проекты слоёв по `.agents/rules/csharp/local-project-layers.md`: `src/domain/EveTrader.Domain`, `src/application/EveTrader.Application`, `src/infrastructure/EveTrader.Infrastructure.{Esi,Facts,Archive,Sde}`, `src/presentation/EveTrader.Cli` — проверка: `dotnet sln EveTrader.slnx list` показывает все проекты, диф списка csproj и содержимого slnx пуст
- [ ] 1.3 Создать тестовые проекты в `tests/unit/`, `tests/integration/`, `tests/architecture/` на фиксированном стеке (xUnit v3, Shouldly, NSubstitute, Coverlet) — проверка: `dotnet test EveTrader.slnx` запускается и завершается успешно
- [ ] 1.4 Закрепить законы слоёв в `tests/architecture/` через NetArchTest (ссылки только вниз, домен без IO, инфраструктурные проекты не ссылаются друг на друга) — проверка: временно добавленная ссылка `EveTrader.Domain` → `EveTrader.Infrastructure.Esi` роняет тест, после удаления тесты зелёные
- [ ] 1.5 Подключить `Microsoft.CodeAnalysis.BannedApiAnalyzers` в `EveTrader.Domain` с запретом `DateTime.UtcNow`, `DateTimeOffset.Now`, `DateTime.Now`, `TimeProvider.System` — проверка: временный вызов роняет сборку, после удаления сборка зелёная
- [ ] 1.6 Завести проектный `ScheduledWorkerBase` в форме из `csharp/background-workers.md` (`AdaptivePollSchedule`, DI-scope на цикл, эмиссия счётчиков по `[Counter]`, политика ошибок, `Describe()`) — проверка: unit-тесты базы покрывают scope на цикл, эмиссию счётчиков при успехе и отказе, продолжение после исключения, отмену
- [ ] 1.7 Завести `CHANGELOG.md` в корне как аналог changelog из `process/docs-completeness.md` — проверка: файл содержит запись об этом изменении со ссылкой на change
- [ ] 1.8 Оформить и отправить наверх в `nova/meta/rules` расхождения внутри набора: определение layered (`architecture.md` против `project-naming-and-setup.md` §1–4 и `project-deps-and-tests.md` §1) и путь фронтенда (`src/frontend/` против `web/` в правилах `typescript/`) — проверка: issue или MR в репозитории правил создан, ссылка записана в `local-project-layers.md`

## 2. Озеро, покрытие, материализация

- [ ] 2.1 Описать схемы Parquet для событий, базовых линий, чекпойнтов, признаков, дневной истории и покрытия; в каждой — время события (момент либо интервал, с явным различением) и время получения, где применимо — версия статических данных — проверка: тест на чтение записанного файла и совпадение схемы с ожидаемой
- [ ] 2.2 Реализовать запись наблюдения: временный файл, атомарное переименование, запись покрытия последней — проверка: integration-тест, что прерывание до записи покрытия оставляет озеро без подтверждённых фактов
- [ ] 2.3 Реализовать определение факта через покрытие: неподтверждённые строки невидимы читателям и убираются при следующем запуске — проверка: integration-тесты сценариев `specs/market-facts/bitemporal-store` §«Факт — строка, подтверждённая покрытием»
- [ ] 2.4 Реализовать идемпотентность приёма — проверка: повторная подача того же наблюдения не меняет состав фактов
- [ ] 2.5 Реализовать журнал покрытия с тремя состояниями и учётом пропусков источника — проверка: integration-тесты всех сценариев `specs/market-observation/coverage-log`
- [ ] 2.6 Реализовать реестр материализованных интервалов и окно локального хранения как настройку — проверка: integration-тесты сценариев `specs/market-facts/materialization` (сдвиг окна помечает интервалы восполнимыми, расширение назад догружает)
- [ ] 2.7 Реализовать чтение через DuckDB **двумя раздельными портами**: порт плоских строк для признаков и обучения, порт агрегатов для отчётов оператору — проверка: тест архитектуры, что домен и вычисление признаков не имеют доступа к порту агрегатов
- [ ] 2.8 Реализовать запрос на момент времени и запрос без момента — проверка: integration-тесты сценариев `bitemporal-store` §«Чтение на момент времени»
- [ ] 2.9 Реализовать отказ на изменение и удаление подтверждённого факта и отказ на срок хранения сырья — проверка: integration-тесты обеих операций
- [ ] 2.10 Реализовать партиционирование по дате наблюдения и региону и сортировку внутри файла — проверка: план запроса DuckDB за интервал читает только партиции интервала

## 3. Дневная история — первый обучающий набор

- [ ] 3.1 Реализовать импорт архива дневной истории на всю доступную глубину, с вежливостью к источнику (ограничение параллелизма, уважение метаданных о времени изменения) — проверка: повторный запуск не скачивает уже загруженное
- [ ] 3.2 Реализовать би-темпоральность дневной истории: партиционирование по времени получения, досинхронизация дополненных задним числом суток — проверка: повторное получение той же календарной даты даёт новую строку, прежняя остаётся доступна
- [ ] 3.3 Посчитать по импортированной истории фактическое число торгуемых типов и активных регионов — проверка: числа записаны в `design.md` §Open Questions, вопрос закрыт
- [ ] 3.4 Реализовать сбор дневной истории из живого ESI через тот же вход — проверка: факты из ESI и из архива неразличимы по схеме и лежат в одном наборе

## 4. Домен: типы, разметка, признаки

- [ ] 4.1 Описать представление ордера и наблюдения: `struct` фиксированного размера, цена целым в сотых долях ISK, время наблюдения аргументом — проверка: unit-тесты на границы масштаба цены, тест на размер структуры
- [ ] 4.2 Реализовать дифф двух наблюдений с разметкой шести видов событий — проверка: unit-тесты покрывают все сценарии `specs/market-observation/order-events` §«Виды событий»
- [ ] 4.3 Реализовать различение времени события и времени наблюдения: точный момент из `issued` для перестановки и пополнения, интервал для исполнения и исчезновения — проверка: unit-тесты сценариев §«Время события отличается от времени наблюдения»
- [ ] 4.4 Реализовать отпечаток NPC и событие пополнения склада — проверка: unit-тесты, что выборка перестановок не содержит пополнений
- [ ] 4.5 Реализовать окно подтверждения исчезновения с пропусками источника — проверка: unit-тесты «подтверждённое исчезновение» и «ордер вернулся до подтверждения»
- [ ] 4.6 Реализовать базовую линию и правило частичного наблюдения (нет исчезновений, счётчик окна не меняется) — проверка: unit-тесты обоих сценариев
- [ ] 4.7 Реализовать вычисление признаков в той же проходке, с охватом признаков как отдельной настройкой — проверка: unit-тесты двустороннего и односторонного стакана; тест, что сужение охвата признаков не влияет на события
- [ ] 4.8 Реализовать восстановление состава стакана из чекпойнта и событий, только по полным наблюдениям — проверка: property-тест «восстановить стакан на время полного наблюдения и пересчитать признаки — совпадает с признаками из проходки»
- [ ] 4.9 Реализовать суточные чекпойнты — проверка: integration-тест, что восстановление любого момента сворачивает не более суток событий
- [ ] 4.10 Замерить проходку на синтетическом стакане в миллион ордеров — проверка: аллокации не растут линейно по числу ордеров, длительность зафиксирована

## 5. Импорт окна ордербука

- [ ] 5.1 Реализовать загрузку архивных снимков интервалами, с реестром и вежливостью — проверка: загрузка суток проходит, реестр отмечает все регионы этого интервала, повторный запуск не скачивает заново
- [ ] 5.2 Реализовать распаковку и разбор архивного формата в то же представление наблюдения, что у живого сбора — проверка: снимок из архива и наблюдение из ESI дают одинаковую структуру наблюдения
- [ ] 5.3 Реализовать объявление шага наблюдения источником и его перенос в покрытие; события перестановки из тридцатиминутных снимков помечаются неполными — проверка: integration-тесты сценариев `specs/market-observation/intake` §«Источник объявляет форму наблюдения»
- [ ] 5.4 Прогнать конвертацию окна порциями по суткам с возможностью продолжить с обрыва — проверка: прерывание и перезапуск не дублируют факты и не теряют суток
- [ ] 5.5 Проверить на архиве семантику `issued`: у ордера с тем же идентификатором `issued` двигается при смене цены и не двигается при уменьшении остатка — проверка: результат замера записан, расхождение с ожиданием останавливает работы по разметке
- [ ] 5.6 Проверить на архиве полноту отпечатка NPC — проверка: после фильтра не остаётся ордеров с двинувшимся `issued` и неизменной ценой; порог идентификатора записан в `design.md`
- [ ] 5.7 Измерить по архиву частоту пропусков источника и выбрать длину окна подтверждения исчезновения — проверка: значение записано в `design.md`, вопрос закрыт
- [ ] 5.8 Проверить видимость рынков игровых структур: присутствует ли локация Перимитера в снимках по The Forge — проверка: результат записан в `design.md`, вопрос закрыт

## 6. Живой сбор по хабам

- [ ] 6.1 Реализовать клиент стакана: пагинация по объявленному числу страниц, время получения на каждую страницу, границы интервала сбора — проверка: integration-тест против записанных ответов, время страниц различается
- [ ] 6.2 Реализовать governor бюджета ошибок, общий на процесс — проверка: unit-тесты на паузу до сброса и возобновление; тест, что бюджет один на все регионы
- [ ] 6.3 Реализовать условные запросы и обработку ответа «не изменилось» — проверка: ответ без изменений даёт запись покрытия и ноль событий
- [ ] 6.4 Реализовать потоковый разбор через `Utf8JsonReader` в переиспользуемый массив структур — проверка: тест на отсутствие аллокаций на ордер и на корректность разбора всех полей
- [ ] 6.5 Реализовать пометку частичного наблюдения с долей полученных страниц — проверка: integration-тест на обрыве посреди пагинации
- [ ] 6.6 Реализовать эмпирическое определение пригодных регионов и политику охвата с записью изменений политики со временем — проверка: integration-тесты сценариев `specs/market-observation/scope-policy`
- [ ] 6.7 Реализовать воркер-диспетчер наблюдения на `ScheduledWorkerBase` с `AdaptivePollSchedule`: каждый цикл берёт регионы с истёкшим сроком годности по заголовкам ответа — проверка: unit-тесты выбора срока (максимум из срока годности и интервала политики), тест на смещение границы поколения кэша
- [ ] 6.8 Реализовать отдельные воркеры для дневной истории и компакции — проверка: тест, что задачи не склеены в один воркер, у каждого свой набор счётчиков
- [ ] 6.9 Реализовать скользящее окно сырых страниц на 48 часов с автоматическим удалением — проверка: страницы старше окна удалены, окно не используется как источник истины ни в одном пути чтения
- [ ] 6.10 Прогнать живой сбор по торговым хабам сутки — проверка: покрытие по хабам не ниже порога, процесс не падает

## 7. Реплей и сверка

- [ ] 7.1 Реализовать порт источника наблюдений и реплей собственного озера через восстановленные наблюдения — проверка: реплей проходит те же стадии, что живой сбор, без ветвлений по типу источника
- [ ] 7.2 Сверить реплей с живым прогоном — проверка: реплей суток даёт тот же набор событий и признаков, что записал живой прогон, с точностью до помеченных неполными
- [ ] 7.3 Проверить детерминированность — проверка: два реплея одного интервала дают идентичный результат, включая порядок строк

## 8. Наблюдаемость и приёмка

- [ ] 8.1 Реализовать метрики по контракту `observability/diagnostics.md`: число запросов, трафик, доля ответов «не изменилось», длительность наблюдения региона, остаток бюджета ошибок, доля изменившихся ордеров за наблюдение, число пропусков источника — проверка: метрики видны в экспорте
- [ ] 8.2 Снять фактические замеры и сопоставить с бюджетом из `design.md`: пиковая память, длительность наблюдения крупнейшего региона, объём событий за сутки, объём после конвертации окна архива — проверка: замеры записаны; расхождение больше чем втрое — повод пересмотреть бюджет, а не удалять сырьё
- [ ] 8.3 Прогнать приёмочный сценарий: перестроить признаки за материализованный интервал из сырья и сверить с записанными — проверка: совпадение полное для полных наблюдений, отчёт о покрытии без незапланированных пробелов
- [ ] 8.4 Реализовать команды `EveTrader.Cli`: запуск сбора, импорт интервала, отчёт о покрытии и материализации, осмотр фактов, реплей — проверка: каждая команда запускается и печатает непустой результат
- [ ] 8.5 Закрыть бумагу изменения — проверка: `openspec validate add-market-observation-pipeline --strict` без замечаний, запись в `CHANGELOG.md` на месте, `dotnet build EveTrader.slnx -c Debug` зелёный
