## 1. Загрузка и разбор

- [ ] 1.1 Реализовать загрузку архивных снимков интервалами, с реестром и вежливостью — проверка: загрузка суток проходит, реестр отмечает все регионы этого интервала, повторный запуск не скачивает заново
- [ ] 1.2 Реализовать распаковку и разбор архивного формата в то же представление наблюдения, что у живого сбора — проверка: integration-тест сценариев `specs/market-facts/materialization` §«Архивные данные приводятся к общей схеме»
- [ ] 1.3 Реализовать объявление шага наблюдения источником и его перенос в покрытие; события перестановки из тридцатиминутных снимков помечаются неполными — проверка: integration-тесты сценариев `specs/market-observation/intake` §«Источник объявляет форму наблюдения»

## 2. Замеры на архиве

- [ ] 2.1 Проверить на архиве семантику `issued`: у ордера с тем же идентификатором `issued` двигается при смене цены и не двигается при уменьшении остатка — проверка: результат замера записан, расхождение с ожиданием останавливает работы по разметке
- [ ] 2.2 Проверить на архиве полноту отпечатка NPC — проверка: после фильтра не остаётся ордеров с двинувшимся `issued` и неизменной ценой; порог идентификатора записан в `design.md`, вопрос закрыт
- [ ] 2.3 Измерить по архиву частоту пропусков источника и выбрать длину окна подтверждения исчезновения — проверка: значение записано в `design.md` и перенесено в настройку, вопрос закрыт
- [ ] 2.4 Проверить видимость рынков игровых структур: присутствует ли локация Перимитера в снимках по The Forge — проверка: результат записан в `design.md`, вопрос закрыт

## 3. Прогон окна

- [ ] 3.1 Прогнать конвертацию окна порциями по суткам с возможностью продолжить с обрыва — проверка: прерывание и перезапуск не дублируют факты и не теряют суток

## 4. Бумага

- [ ] 4.1 Закрыть бумагу изменения — проверка: `openspec validate add-orderbook-archive-import --strict` без замечаний, запись в `CHANGELOG.md` на месте, `dotnet build EveTrader.slnx -c Debug` зелёный
