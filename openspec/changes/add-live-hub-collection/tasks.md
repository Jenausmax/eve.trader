## 1. Клиент источника

- [ ] 1.1 Реализовать клиент стакана: пагинация по объявленному числу страниц, время получения на каждую страницу, границы интервала сбора — проверка: integration-тест против записанных ответов, время страниц различается
- [ ] 1.2 Реализовать governor бюджета ошибок, общий на процесс — проверка: unit-тесты на паузу до сброса и возобновление; тест, что бюджет один на все регионы
- [ ] 1.3 Реализовать условные запросы и обработку ответа «не изменилось» — проверка: ответ без изменений даёт запись покрытия и ноль событий
- [ ] 1.4 Реализовать потоковый разбор через `Utf8JsonReader` в переиспользуемый массив структур — проверка: тест на отсутствие аллокаций на ордер и на корректность разбора всех полей
- [ ] 1.5 Реализовать пометку частичного наблюдения с долей полученных страниц — проверка: integration-тест на обрыве посреди пагинации

## 2. Охват

- [ ] 2.1 Реализовать эмпирическое определение пригодных регионов и политику охвата с записью изменений политики со временем — проверка: integration-тесты сценариев `specs/market-observation/scope-policy`

## 3. Воркеры

- [ ] 3.1 Реализовать воркер-диспетчер наблюдения на `ScheduledWorkerBase` с `AdaptivePollSchedule`: каждый цикл берёт регионы с истёкшим сроком годности по заголовкам ответа — проверка: unit-тесты выбора срока (максимум из срока годности и интервала политики), тест на смещение границы поколения кэша
- [ ] 3.2 Реализовать отдельные воркеры для дневной истории и компакции — проверка: тест, что задачи не склеены в один воркер, у каждого свой набор счётчиков
- [ ] 3.3 Реализовать скользящее окно сырых страниц на 48 часов с автоматическим удалением — проверка: страницы старше окна удалены, окно не используется как источник истины ни в одном пути чтения

## 4. Прогон

- [ ] 4.1 Прогнать живой сбор по торговым хабам сутки — проверка: покрытие по хабам не ниже порога, процесс не падает

## 5. Бумага

- [ ] 5.1 Закрыть бумагу изменения — проверка: `openspec validate add-live-hub-collection --strict` без замечаний, запись в `CHANGELOG.md` на месте, `dotnet build EveTrader.slnx -c Debug` зелёный
