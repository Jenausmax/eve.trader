## 1. Реплей

- [ ] 1.1 Реализовать порт источника наблюдений и реплей собственного озера через восстановленные наблюдения — проверка: реплей проходит те же стадии, что живой сбор, без ветвлений по типу источника; домен и свёртка не изменены

## 2. Сверка

- [ ] 2.1 Сверить реплей с живым прогоном — проверка: реплей суток даёт тот же набор событий и признаков, что записал живой прогон, с точностью до помеченных неполными
- [ ] 2.2 Проверить детерминированность — проверка: integration-тест сценария `specs/market-observation/intake` §«Реплей детерминирован»: два реплея одного интервала дают идентичный результат, включая порядок строк

## 3. Бумага

- [ ] 3.1 Закрыть бумагу изменения — проверка: `openspec validate add-replay-source --strict` без замечаний, запись в `CHANGELOG.md` на месте, `dotnet build EveTrader.slnx -c Debug` зелёный
