## 1. Схемы и раскладка

- [ ] 1.1 Зафиксировать раскладку озера и общие соглашения схемы: время события (момент либо интервал, с явным различением), время получения, версия статических данных — проверка: тест на чтение записанного файла и совпадение схемы с ожидаемой
- [ ] 1.2 Описать схемы Parquet для покрытия и реестра материализации — проверка: тест на чтение записанного файла и совпадение схемы с ожидаемой
- [ ] 1.3 Реализовать партиционирование по дате наблюдения и региону и сортировку внутри файла — проверка: план запроса DuckDB за интервал читает только партиции интервала

## 2. Определение факта и идемпотентность

- [ ] 2.1 Реализовать запись наблюдения: временный файл, атомарное переименование, запись покрытия последней — проверка: integration-тест, что прерывание до записи покрытия оставляет озеро без подтверждённых фактов
- [ ] 2.2 Реализовать определение факта через покрытие: неподтверждённые строки невидимы читателям и убираются при следующем запуске — проверка: integration-тесты сценариев `specs/market-facts/bitemporal-store` §«Факт — строка, подтверждённая покрытием»
- [ ] 2.3 Реализовать идемпотентность приёма — проверка: integration-тесты сценариев `specs/market-observation/intake` §«Приём идемпотентен»; повторная подача того же наблюдения не меняет состав фактов
- [ ] 2.4 Реализовать отказ на изменение и удаление подтверждённого факта и отказ на срок хранения сырья — проверка: integration-тесты обеих операций

## 3. Покрытие и материализация

- [ ] 3.1 Реализовать журнал покрытия с тремя состояниями и учётом пропусков источника — проверка: integration-тесты всех сценариев `specs/market-observation/coverage-log`
- [ ] 3.2 Реализовать реестр материализованных интервалов и окно локального хранения как настройку — проверка: integration-тесты сценариев `specs/market-facts/materialization` (сдвиг окна помечает интервалы восполнимыми, расширение назад отмечает недостающие к загрузке)

## 4. Чтение

- [ ] 4.1 Реализовать чтение через DuckDB **двумя раздельными портами**: порт плоских строк для признаков и обучения, порт агрегатов для отчётов оператору — проверка: тест архитектуры, что домен и вычисление признаков не имеют доступа к порту агрегатов
- [ ] 4.2 Реализовать запрос на момент времени и запрос без момента — проверка: integration-тесты сценариев `bitemporal-store` §«Чтение на момент времени»

## 5. Бумага

- [ ] 5.1 Закрыть бумагу изменения — проверка: `openspec validate add-bitemporal-fact-lake --strict` без замечаний, запись в `CHANGELOG.md` на месте, `dotnet build EveTrader.slnx -c Debug` зелёный
