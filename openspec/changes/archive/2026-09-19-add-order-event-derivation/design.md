## Context

Общий контекст конвейера — в `add-bitemporal-fact-lake/design.md` §Context. Из четырёх
внешних фактов, описанных там, на этот чейндж работают два: `issued` как таймштамп
последней правки (а не даты создания) и пятиминутное ограничение игры на правку ордера.
Всё остальное здесь — следствия.

## Goals / Non-Goals

**Goals:**

- Уложить свёртку миллиона ордеров в бюджет наблюдения без пауз GC.
- Разметить события так, чтобы обучение не путало действия конкурентов с движениями NPC.
- Не соврать о том, что известно: точный момент там, где он есть, интервал там, где его
  нет.
- Оставить сырьё пригодным для признаков, которые ещё не придуманы.

**Non-Goals:**

- Источники наблюдений — приедут своими чейнджами.
- Оптимизация под объёмы, которых не будет. Одна машина, один процесс.

## Decisions

### Одна проходка вместо двух

Дифф с предыдущим наблюдением, разметка вида события и вычисление признаков — **один
проход по стакану в памяти**, не два и не запрос к хранилищу.

Полный стакан региона всё равно должен быть в памяти: дифф без него невозможен. Пока мы
по нему идём, разметка и признаки получаются попутно.

Альтернатива — считать признаки запросом после записи событий — отвергнута: каждый такой
запрос тянет через процесс миллионы строк на каждом наблюдении.

### Разметка вида события

```
                    ┌─ issued изменился ─┬─ цена изменилась ──▶ перестановка цены
                    │                    │                      (время события = issued)
ордер есть в обоих ─┤                    └─ цена та же ────────▶ пополнение склада NPC
    наблюдениях     │                                            (если отпечаток NPC)
                    └─ issued тот же ────── остаток меньше ────▶ наблюдённое исполнение
                                                                  (время = интервал)
```

**Отпечаток NPC обязателен, а не желателен.** Ордера NPC узнаются по совокупности:
идентификатор ниже порога, длительность 365 суток, остаток равен полному объёму, круглая
цена, сторона продажи. При пополнении склада им переставляют `issued` без изменения цены;
без фильтра каждое пополнение попадёт в обучение как действие конкурента, причём именно
по тем предметам, которыми торгуют новички.

**Появление даёт момент создания только при пятиминутном шаге.** Новый ордер не может
быть отредактирован в первые пять минут жизни, поэтому при шаге пять минут `issued` у
впервые увиденного идентификатора — это момент создания. При тридцатиминутном шаге такой
гарантии нет, и `issued` остаётся моментом последней правки.

### Исчезновение подтверждается окном

Источник допускает пропуски: ордер исчезает из одного снимка и возвращается в следующем,
хотя идентификаторы уникальны и не переиспользуются. Исчезновение по единственному
сравнению — систематическая выдумка на дефектах источника, и выдумка эта выглядит именно
как исполнение.

Поэтому исчезновение — состояние процесса до подтверждения и факт после. Время события
указывает интервал между последним наблюдением ордера и первым отсутствием, время
наблюдения — момент подтверждения. Пропуски учитываются в покрытии: их частота и есть
основание для выбора длины окна.

Стартовое значение длины окна — **два последовательных полных наблюдения**. Уточняется
замером частоты пропусков на архиве в `add-orderbook-archive-import`; длина окна —
настройка, а не константа, поэтому уточнение не требует переделки.

### Формат представления в горячем пути

Миллион записей каждые пять минут не допускает объектной модели.

| | решение | почему не иначе |
|---|---|---|
| представление ордера | `struct` фиксированного размера в массиве | миллион объектов на наблюдение — GC-паузы вместо работы |
| буферы | два поколения массивов на регион, переиспользуются | новый массив на наблюдение фрагментирует LOH |
| индекс по идентификатору | открытая адресация поверх массива | `Dictionary` на миллион записей — десятки МБ накладных и худшая локальность |
| разбор ответа | потоковый разбор через `Utf8JsonReader` прямо в массив | `JsonSerializer` в коллекцию — тот же миллион объектов |
| цена | целое в сотых долях ISK | `double` теряет точность на сравнении цен, `decimal` слишком дорог в цикле |

Оценка памяти: миллион ордеров при ~64 байтах — 64 МБ на поколение, два поколения плюс
индекс — порядка 200 МБ на полный охват. Это единственное место, где код пишется
«неудобно, зато без аллокаций».

Потоковый разбор ответа ESI живёт в `add-live-hub-collection` — здесь фиксируется
представление, в которое он разбирает, чтобы оба чейнджа не изобрели своё.

### Суточные чекпойнты

Без чекпойнтов восстановление состояния на момент сворачивает всю историю региона — через
год это миллиарды строк на один запрос, то есть требование записано, а исполнить его
нельзя.

Суточный чекпойнт полного стакана региона стоит около миллиона строк в сутки на полный
охват, порядка 10–15 МБ сжатого — на фоне событий шум. Часовой чекпойнт даёт в двадцать
четыре раза больше и не нужен: бэктест не спрашивает точку, он проходит интервал подряд.

### Свёртка проверяется без источников

Источников наблюдений в этом чейндже нет, и свёртка проверяется синтетическими парами
наблюдений: для каждого вида события — пара, которая его порождает, и пара, которая не
должна. Это не заглушка вместо проверки, а более строгая проверка: на живом потоке
редкие виды событий приходится ждать, а пополнение склада NPC на синтетике
конструируется точно.

Сверка с настоящими данными — в `add-orderbook-archive-import` (семантика `issued` и
полнота отпечатка NPC на архиве) и в `add-replay-source` (реплей против живого прогона).

## Замеры проходки

Сняты на синтетическом стакане, признаки выключены — мерился дифф.

| Величина | Замер | Что закладывалось |
|---|---|---|
| аллокации на наблюдение, 250 тыс. ордеров | 504 байта | не растут линейно по числу ордеров |
| аллокации на наблюдение, 1 млн ордеров | 504 байта | то же |
| длительность проходки, 1 млн ордеров | 294 мс | уложиться в бюджет наблюдения |
| размер `OrderSnapshot` | 56 байт | ~64 байта |

Аллокации не зависят от размера стакана вовсе — это и требовалось: буферы и индекс
переиспользуются, на ордер не аллоцируется ничего.

**Первый замер этого не показал.** До него проходка аллоцировала 25 МБ на миллион
ордеров, ровно линейно: наблюдатель создавал индекс текущего наблюдения заново каждый
раз. Решение о переиспользовании буферов было записано в бумаге и не исполнено в коде;
поймал это замер, а не ревью. Отсюда и форма проверки: тест сравнивает аллокации на двух
размерах стакана, а не просто печатает число.

## Risks / Trade-offs

- **Отпечаток NPC может оказаться неполным** → выборка перестановок будет содержать
  пополнения складов, и модель поведения конкурентов обучится на движениях NPC. Проверка
  дешёвая и обязательная, но выполняется на архиве в `add-orderbook-archive-import`: на
  синтетике полноту отпечатка проверить нельзя по определению.
- **Длина окна подтверждения исчезновения не известна** → слишком короткое даёт ложные
  исчезновения, слишком длинное задерживает факт. Стартуем с двух наблюдений, уточняем
  замером; окно — настройка.
- **Порванный срез неустраним** → не устраняем, а перестаём претендовать: время
  фиксируется на страницу, наблюдение несёт границы интервала. Для перестановок это
  лечится `issued`, для исполнений остаётся интервалом.
- **Горячий путь пишется без аллокаций и потому неудобен** → цена сознательная и
  локализованная: неудобство ограничено проходкой, всё остальное пишется нормально.
- **Схема события фиксируется до первого настоящего потока** → если на архиве вылезет
  поле, которого не хватает, менять придётся уже записанное. Смягчение: свёртка
  перестраиваема из сырья, а сырьё в пределах окна не удаляется.

## Migration Plan

Миграции нет. Откат — удаление наборов `order-events`, `order-baselines`,
`book-checkpoints`, `book-features` и их записей покрытия; дневная история и озеро
остаются целыми.

### Свёртка разделена на состояние и функции

`class-layout-and-tooling.md` §1a запрещает приватные методы в продакшн-коде, и свёртка
под него сначала не подошла: наблюдатель — последовательный обработчик с состоянием, и
разметка естественно писалась его приватными методами.

Разделено на три типа: `ObserverState` — что наблюдатель помнит между наблюдениями,
`OrderDiff` — статические функции разметки над этим состоянием, `RegionObserver` — только
порядок шагов одного наблюдения.

Выигрыш сверх соблюдения правила: видно, что именно наблюдатель обязан помнить, и почему
вообще помнит — исчезновение выводится не из пары снимков, а из нескольких подряд. Цена —
состояние в сигнатурах функций разметки; аллокаций это не добавило, замер после разделения
тот же.

## Что вскрылось при реализации

**Перестановка цены без изменения цены.** Таблица видов событий покрывает
`issued` изменился + цена изменилась (перестановка) и `issued` изменился + цена та же +
отпечаток NPC (пополнение склада). Случай «`issued` изменился, цена та же, отпечатка NPC
нет» в ней не назван, а в игре возможен: владелец переставил ордер на ту же цену.

Взято: перестановка с равными ценами. Это действие владельца, то есть действие
конкурента, и выбрасывать его нельзя; отнести к пополнению склада тоже нельзя — ордер не
NPC. Отдельный вид ради этого не заводится: событий шесть, и седьмое сломало бы главное
утверждение спеки.

**Восстановление отстаёт на окно подтверждения.** Состав стакана, восстановленный на
момент, отражает *подтверждённое* знание: ордер, пропавший на последнем наблюдении, ещё
числится живым, потому что исчезновение до подтверждения окном фактом не является. Это
прямое следствие принятого решения, а не дефект, и property-тест сверяет состав там, где
все ожидающие подтверждения разрешились.

## Open Questions

- **Пороги глубины стакана.** Какие отклонения от лучшей цены считать (1 %, 5 %, ещё?).
  Набор порогов — параметр вычисления признаков; менять дёшево, пока признаки
  перестраиваются из сырья.
- **Точная длина окна подтверждения исчезновения.** Стартовое значение — два
  последовательных полных наблюдения; уточняется по частоте пропусков в архиве
  (`add-orderbook-archive-import`).
- **Полнота отпечатка NPC.** Порог идентификатора и совокупность признаков подтверждаются
  на архиве (`add-orderbook-archive-import`).
