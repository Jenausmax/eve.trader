## 1. Процесс API

- [ ] 1.1 Завести `src/presentation/EveTrader.Api` (minimal API, только чтение), зарегистрировать в `EveTrader.slnx` по `process/project-slnx-registration.md` — проверка: `dotnet build EveTrader.slnx -c Debug` зелёный, проект виден в решении
- [ ] 1.2 Собрать обвязку озера на процесс: layout, журнал покрытия, реестр материализации, оба читателя, телеметрия; путь к озеру разрешается по `process/config-resolution.md` и печатается при старте — проверка: процесс поднимается на озере приёмки и печатает разрешённый путь
- [ ] 1.3 Развести жизненные циклы: обвязка живёт с процессом, соединение с хранилищем открывается на запрос — проверка: integration-тест выполняет два параллельных запроса к одному озеру и оба отвечают
- [ ] 1.4 Подтвердить независимость процессов сценарием `api/query-surface` §«Сбор остановлен» и §«Сбор идёт во время чтения» — проверка: integration-тест отвечает на запрос при незапущенном сборе и не видит недописанных партиций при записи во время чтения

## 2. Поверхность запроса фактов

- [ ] 2.1 Пути чтения фактов с обязательным интервалом и необязательным моментом знания — проверка: сценарии `api/query-surface` §«Чтение на момент времени доступно через поверхность» зелёными тестами
- [ ] 2.2 Форма ответа несёт версию статических данных, наблюдение и пометку неполноты — проверка: сценарии §«Ответ называет происхождение и полноту»
- [ ] 2.3 Состояние покрытия интервала в каждом ответе, включая пустой — проверка: сценарии §«Отсутствие данных отличимо от отсутствия наблюдения»
- [ ] 2.4 Ограничение объёма ответа с признаком продолжения; запрос без границ интервала отклоняется — проверка: сценарии §«Объём выдачи ограничен»
- [ ] 2.5 Отсутствие пишущих путей закреплено тестом по описанию контракта — проверка: сценарий §«Пишущего пути нет»

## 3. Операционная поверхность

- [ ] 3.1 Покрытие за интервал как ресурс с тремя состояниями и источником восполнения — проверка: сценарии `api/operations-surface` §«Покрытие за интервал доступно как ресурс»
- [ ] 3.2 Реестр материализации как ресурс — проверка: сценарий §«Запрос материализации за период»
- [ ] 3.3 Расход ресурсов и здоровье сбора как ресурс; несообщённая величина помечается несообщённой, а не нулём — проверка: сценарии §«Здоровье сбора и расход ресурсов доступны как ресурс»
- [ ] 3.4 Отчёты отличимы от плоских фактов в самом контракте — проверка: сценарий §«Агрегация операционных ресурсов допустима и помечена»

## 4. Контракт и клиент

- [ ] 4.1 Машиночитаемое описание контракта порождается из кода и коммитится в репозиторий — проверка: сценарий §«Описание доступно»; файл описания в репозитории, содержит все пути поверхности
- [ ] 4.2 Клиент TypeScript порождается из описания шагом сборки, а не запросом к живому серверу — проверка: сценарий §«Контракт изменился»; правка формы ответа ломает `bun run typecheck`

## 5. Каркас фронтенда

- [ ] 5.1 Завести `web/` по канону `.agents/rules/typescript/`: bun workspace, `bun.lock` в `web/`, React 19 + Vite, Tailwind 4, TanStack Query/Router, i18next с локалями `ru` и `en` — проверка: гейт из `web/` — `bun run typecheck`, `bun run lint`, `bun run test` — все три с нулевым выходом
- [ ] 5.2 Подключить порождённый клиент и `QueryClientProvider`, ключи запросов по `typescript/tanstack-query-and-router.md` — проверка: тестовый запрос к поверхности проходит через порождённый клиент, гейт зелёный
- [ ] 5.3 Выбрать и подключить `lightweight-charts`; замерить отрисовку на реальном ряду из озера приёмки — проверка: замер записан в `design.md`; при превышении порога отзывчивости включается запасной вариант из §«Графики»

## 6. Экраны состояния

- [ ] 6.1 Экран покрытия: три состояния различимы видом, легенда называет каждое, выбор восполнимого пробела показывает источник — проверка: сценарии `web-console/operations-views` §«Экран покрытия различает три состояния»
- [ ] 6.2 Экран материализации с источником, временем загрузки и границей окна хранения — проверка: сценарий §«Запрошен период»
- [ ] 6.3 Экран расхода ресурсов и здоровья сбора; несообщённая величина видна как несообщённая — проверка: сценарии §«Экран расхода ресурсов и здоровья сбора»
- [ ] 6.4 Управляющих действий на экранах состояния нет — проверка: сценарий §«Консоль ничего не изменяет»

## 7. Экраны рынка

- [ ] 7.1 Обзор наблюдаемых рынков и переход к детализации пары — проверка: сценарии `web-console/market-views` §«Обзор рынка и детализация по паре»
- [ ] 7.2 Ряды дневной истории и состояние стакана на экране пары: лучшие цены обеих сторон, спред, глубина — проверка: тот же раздел, оба сценария
- [ ] 7.3 Пропуск изображается пропуском: разрыв вместо прямой, отсутствие стороны вместо нуля, спред не показан при односторонном стакане — проверка: сценарии §«Пропуск изображается пропуском»
- [ ] 7.4 Момент наблюдения, версия статических данных и пометка неполноты видны на экране — проверка: сценарии §«Происхождение показанного видно»
- [ ] 7.5 Отзывчивость на месячном интервале частого наблюдения; шаг прореживания объявлен на экране — проверка: сценарии §«Ряды выдерживают объём наблюдений»

## 8. Бумага

- [ ] 8.1 Закрыть бумагу изменения — проверка: `openspec validate add-web-console --strict` без замечаний, запись в `CHANGELOG.md` на месте, `dotnet build EveTrader.slnx -c Debug` зелёный и гейт фронтенда из `web/` зелёный
