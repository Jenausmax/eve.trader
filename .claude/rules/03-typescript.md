# TypeScript / frontend

Канон: [`.agents/rules/typescript/`](../../.agents/rules/typescript/)

Применяется, если у проекта появится веб-морда. Сейчас фронтенда нет —
раздел висит про запас, правила грузятся по `globs` (`**/*.ts`, `**/*.tsx`).
